# Simple-Number-Guessing-Game
 a number guessing game where the program generates a random number, and the user has to guess it.  Use Java’s Random class to generate a random number.  Give feedback to the user (e.g., “higher,” “lower”). Limit the number of attempts and handle invalid inputs.  Skills Covered: Random number generation, loops, conditional st.
